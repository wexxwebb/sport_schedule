<?xml version="1.0" encoding="UTF-8"?>


<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:context="http://www.springframework.org/schema/context"

             xsi:schemaLocation="http://www.springframework.org/schema/security
                http://www.springframework.org/schema/security/spring-security-4.2.xsd
                http://www.springframework.org/schema/beans
                http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
                http://www.springframework.org/schema/context
                http://www.springframework.org/schema/context/spring-context.xsd">

    <global-method-security secured-annotations="enabled"/>

    <http auto-config="true">
        <intercept-url pattern="/" access="permitAll()"/>
        <!--<intercept-url pattern="/showTime**" access="hasRole('ADMIN')"/>-->
        <intercept-url pattern="/inner/**" access="isAuthenticated()"/>

        <form-login
            login-page="/auth"
            username-parameter="login"
            password-parameter="password"
            default-target-url="/inner/dashboard"
            always-use-default-target="true"
            authentication-failure-url="/auth?error=access_denied"/>
        <logout/>
        <csrf disabled="true"/>
    </http>

    <authentication-manager>
        <!--<authentication-provider ref="myCustomAuthProvider">-->
        <authentication-provider>
            <!--<password-encoder ref="encoder"/>-->
            <!--<jdbc-user-service data-source-ref="dataSourse"-->
            <!--users-by-username-query="SELECT login AS username, password, enabled FROM user_data WHERE login = ?"-->
            <!--authorities-by-username-query="SELECT login AS username, role FROM user_data WHERE login = ?"></jdbc-user-service>-->
            <user-service>
                <user name="admin" password="admin" authorities="ROLE_USER, ROLE_ADMIN" />
                <user name="user" password="user" authorities="ROLE_USER" />
            </user-service>
        </authentication-provider>
    </authentication-manager>


</beans:beans>